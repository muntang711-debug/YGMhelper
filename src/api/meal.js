import{requestNeis,getRows,getResult,getTargetSchool,response,validDate,parseMenu}from"./lib/neis.js";
export async function handleMeal(request,env){const date=new URL(request.url).searchParams.get("date")||"";if(!validDate(date))return response({ok:false,message:"Invalid date."},400);try{const school=await getTargetSchool(env),data=await requestNeis("mealServiceDietInfo",{ATPT_OFCDC_SC_CODE:school.officeCode,SD_SCHUL_CODE:school.schoolCode,MLSV_YMD:date},env);if(getResult("mealServiceDietInfo",data)?.CODE==="INFO-200")return response({ok:true,date,meals:[]},200,300);return response({ok:true,date,meals:getRows("mealServiceDietInfo",data).map(row=>({mealName:row.MMEAL_SC_NM||"",date:row.MLSV_YMD||date,menu:parseMenu(row.DDISH_NM),calories:row.CAL_INFO||"",nutrition:row.NTR_INFO||"",origin:row.ORPLC_INFO||""}))},200,300)}catch(error){return response({ok:false,message:"Meal information is temporarily unavailable."},502)}}